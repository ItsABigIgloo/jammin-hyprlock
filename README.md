the hyprlock file will replace your file in ~/.config/hypr/

the whatsong.sh will need to placed in a folder titled "scripts". path should be ~/.config/scripts/

then you should be good to go!

"passowrd" box is sized for a 4 digit pin number. if you put the wrong pin in- you will get "dumbass" with the failure count.
